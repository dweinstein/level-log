var without = require('lodash').without;

//
// Given a list of attributes and an object, return
// a list of 'extra' attributes the object has.
//
module.exports = function extra(attrs, obj) {
  return without.apply(without, [Object.keys(obj)].concat(attrs));
};
